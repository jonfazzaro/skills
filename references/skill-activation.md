# Skill activation announcements

Read this when a skill supplies a Context marker.

When that skill activates, begin the first commentary update with the marker
from its `SKILL.md`, then give a concise `Using <skill name> ...` announcement.
Do not repeat that marker in later updates. If a different skill activates,
announce the newly active skill with its own marker.
